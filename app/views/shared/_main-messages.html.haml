.main_center__box1
  %p
    %span.user_name
      = message.user.name
    %span.time
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
    .reply
      = message.body
    .image
      - if message.image.present?
        = image_tag message.image.to_s
    / .data-message-id
    /   - message.id
